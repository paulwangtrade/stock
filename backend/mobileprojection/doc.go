// Package mobileprojection builds a Desktop-side MobileObservationProjection
// from read-only observation inputs (PortfolioObservation, AIInsight, DailyAttention).
//
// Default OFF (Enabled=false). No Cloud upload, no trading-chain imports,
// no TradePlan / Execution / Broker writes.
package mobileprojection
